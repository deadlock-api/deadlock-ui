---
sidebar_position: 2
---

import LivePreview from '@site/src/components/LivePreview';

# dl-item-card

Displays a single item card with tooltip on hover or click.

## Live Preview

<LivePreview html={`
  <div style="display: flex; gap: 8px; justify-content: center;">
    <dl-item-card class-name="upgrade_aoe_root" hover-effect="scale"></dl-item-card>
    <dl-item-card class-name="upgrade_mystic_regeneration" hover-effect="scale"></dl-item-card>
    <dl-item-card class-name="upgrade_chonky" hover-effect="scale"></dl-item-card>
  </div>
`} />

## Usage

```html
<dl-item-card class-name="upgrade_aoe_root"></dl-item-card>
<dl-item-card class-name="upgrade_mystic_regeneration"></dl-item-card>
<dl-item-card class-name="upgrade_chonky"></dl-item-card>
```

## Properties

| Attribute | Type | Default | Description |
|---|---|---|---|
| `class-name` | `string` | — | Item class name (e.g. `"upgrade_clip_size"`) |
| `item-id` | `number` | — | Item ID (alternative to `class-name`) |
| `hover-effect` | `"none"` \| `"scale"` | `"none"` | Hover effect on the card |
| `show-tier-badge` | `boolean` | — | Override the global `show-tier-badge` for this card |
| `item-name-language` | [`Language`](/docs/types#language) | — | Override language for the item name only. Tooltip content uses the global language set via [`dl-provider`](/docs/components/dl-provider) |
| `tooltip-trigger` | `"hover"` \| `"click"` \| `"none"` | — | Override the tooltip trigger for this card. Falls back to the global [`dl-provider`](/docs/components/dl-provider) value |
| `component-items-data` | `ComponentItemInfo[]` | — | Pre-resolved component items to display in the tooltip. When omitted, resolved automatically |

## Events

| Event | Detail | Description |
|---|---|---|
| `tooltipOpen` | `string` | Emitted when the tooltip opens. Detail is the item's `class_name` |
| `tooltipClose` | `string` | Emitted when the tooltip closes. Detail is the item's `class_name` |

## Hover Effect

<LivePreview html={`
  <div style="display: flex; gap: 24px; justify-content: center; align-items: center;">
    <div style="text-align: center;">
      <dl-item-card class-name="upgrade_clip_size" hover-effect="none"></dl-item-card>
      <p style="color: #8b949e; font-size: 12px; margin-top: 8px;">hover-effect="none"</p>
    </div>
    <div style="text-align: center;">
      <dl-item-card class-name="upgrade_clip_size" hover-effect="scale"></dl-item-card>
      <p style="color: #8b949e; font-size: 12px; margin-top: 8px;">hover-effect="scale"</p>
    </div>
  </div>
`} />

## Tier Badge

The tier badge can be controlled globally via `dl-provider` or per card:

```html
<!-- Global: all cards show badge -->
<dl-provider show-tier-badge="true">
  <dl-item-card class-name="upgrade_clip_size"></dl-item-card>
</dl-provider>

<!-- Per card: override global setting -->
<dl-item-card class-name="upgrade_clip_size" show-tier-badge="true"></dl-item-card>
```

## Item Name Language

Override the displayed item name language independently from the global language. Useful for showing item names in the user's language while keeping tooltip content in another.

<LivePreview html={`
  <div style="display: flex; gap: 8px; justify-content: center;">
    <dl-item-card class-name="upgrade_magic_slow" item-name-language="brazilian" hover-effect="scale"></dl-item-card>
    <dl-item-card class-name="upgrade_magic_slow" item-name-language="japanese" hover-effect="scale"></dl-item-card>
    <dl-item-card class-name="upgrade_magic_slow" item-name-language="koreana" hover-effect="scale"></dl-item-card>
  </div>
`} />

```html
<dl-item-card class-name="upgrade_magic_slow" item-name-language="brazilian"></dl-item-card>
<dl-item-card class-name="upgrade_magic_slow" item-name-language="japanese"></dl-item-card>
<dl-item-card class-name="upgrade_magic_slow" item-name-language="koreana"></dl-item-card>
```

## Custom Trigger (Slot)

You can replace the default card with any custom content using a slot. The tooltip behavior (hover, click, positioning) works the same regardless of what you put inside.

<LivePreview html={`
  <div style="display: flex; gap: 12px; align-items: center;">
    <dl-item-card class-name="upgrade_aoe_root" tooltip-trigger="click">
      <button style="padding: 8px 16px; border-radius: 6px; border: 1px solid #555; background: #2a2a3e; color: #fff; cursor: pointer;">
        Click me
      </button>
    </dl-item-card>
    <dl-item-card class-name="upgrade_mystic_regeneration">
      <span style="color: #b07fd6; text-decoration: underline; cursor: pointer;">Hover me</span>
    </dl-item-card>
  </div>
`} />

```html
<!-- Click trigger -->
<dl-item-card class-name="upgrade_aoe_root" tooltip-trigger="click">
  <button>Click me</button>
</dl-item-card>

<!-- Hover trigger (default) -->
<dl-item-card class-name="upgrade_mystic_regeneration">
  <span>Hover me</span>
</dl-item-card>
```
